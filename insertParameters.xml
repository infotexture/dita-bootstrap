<dummy xmlns:if="ant:if" xmlns:unless="ant:unless">
  <param name="BOOTSTRAP_ICONS_CDN" expression="${icons.cdn.path}"
         if:set="icons.cdn.path"/>
  <param name="BOOTSTRAP_ICONS_INCLUDE" expression="${icons.include}"
         if:set="icons.include"/>
</dummy>
